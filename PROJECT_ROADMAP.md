# 梦幻（Menghuan）项目路线图

## 1. 阶段治理总则

1. 一次只执行一个明确授权的阶段或 D 部署子阶段；
2. 每个开发阶段完成后，必须先由项目总指挥验收；
3. 只有验收通过，项目总指挥才可把对应部署指令交给 Codex；
4. 开发 ChatGPT 不操作远程 GitHub，不部署；
5. Codex 不擅自修改业务逻辑，发现问题时停止并报告；
6. 每个阶段必须定义目标、执行对象、交付物、禁止事项、验收标准和回滚点；
7. 未验收、条件未满足或出现越界时，回滚到最近一个已验收 Git 提交或已验收文档基线；
8. YUC、AGE 和其他来源必须分别独立评估与实现；
9. 首版不开发账户、登录、Session 或多设备同步；
10. Redis 不是阶段1强制依赖，只有真实需求证明必要时才另行批准。

## 2. 阶段0——项目定义与技术方案

### 执行对象

开发 ChatGPT。

### 目标

确定项目定位、功能范围、技术能力、初版技术方案、数据模型方向、环境关系、测试策略和合规边界。

### 交付物

- `PROJECT_CHARTER.md`；
- `TECHNICAL_PROPOSAL.md`；
- `DATA_MODEL_PROPOSAL.md`；
- `PROJECT_ROADMAP.md`；
- `DEVELOPMENT_RULES.md`。

### 禁止事项

- 创建应用、依赖、数据库、Git 仓库或部署；
- 编写业务代码；
- 访问或接入外部来源；
- 创建第六份阶段文档。

### 验收标准

- 五份文档齐全；
- 产品范围、技术边界、数据模型和合规原则清晰；
- 项目独立性明确；
- 未产生工程或部署产物。

### 回滚点

回滚到阶段0开始前的空项目定义状态。

## 3. 阶段0R——项目决策与交接流程修订

### 执行对象

开发 ChatGPT。

### 目标

落实最终技术决策、首版本地存储策略、默认时区、图片策略、AGE 边界、三方职责和代码包交接流程。

### 交付物

仅修订现有五份 Markdown 文档。

### 禁止事项

- 创建第六份文档；
- 创建工程、依赖、数据库或 Git 仓库；
- 写业务代码；
- 访问 YUC、AGE 或其他外部来源；
- 部署或开始阶段0G。

### 验收标准

- TypeScript、Next.js App Router、pnpm workspace、PostgreSQL、Drizzle、Zod、独立 Worker、PostgreSQL 搜索、Vitest 和 Playwright 已正式确定；
- Redis 明确为后续可选；
- 首版明确无账户并使用本地存储；
- 默认北京时间并允许切换日本时间；
- 图片与 AGE 资源边界明确；
- 路线图包含0G及所有D部署子阶段；
- 三方职责和代码包交接规则明确；
- 仍只有五份 Markdown 文档。

### 回滚点

回滚到阶段0初次验收提交或其文档快照。

## 4. 阶段0G——GitHub私有仓库与文档基线

### 执行对象

Codex。

### 目标

建立“梦幻”唯一私有 GitHub 仓库和阶段0文档基线。

### 交付物

- 新建 GitHub 私有仓库；
- 提交并推送五份已验收文档；
- 配置基础分支保护、访问控制和必要仓库设置；
- 输出仓库地址、基线提交哈希和操作报告；
- 导出供阶段1使用的基线代码包。

### 禁止事项

- 创建应用骨架；
- 安装依赖；
- 编写业务代码；
- 创建部署；
- 接入外部来源；
- 擅自增加仓库协作者或改变可见性。

### 验收标准

- 仓库为私有；
- 仅包含获批文档和必要仓库元数据；
- 基线提交可核验；
- 保护规则符合项目总指挥授权；
- 没有应用、依赖或部署产物。

### 回滚点

删除或恢复到项目总指挥指定的空私有仓库状态；如仓库创建本身被撤销，由项目总指挥授权删除。

## 5. 阶段1——全新工程初始化与质量基线

### 执行对象

开发 ChatGPT。

### 目标

基于 Codex 提供的阶段0G基线代码包，从零创建 Next.js App Router 与 pnpm workspace 工程骨架和质量基线。

### 交付物

- `apps/web` 与 `apps/sync-worker` 骨架；
- TypeScript、pnpm workspace、Drizzle、Zod 配置；
- Vitest 与 Playwright 基线；
- Lint、格式化、类型检查和构建脚本；
- `.env.example` 与 Zod 环境变量校验；
- 健康页和健康检查；
- 完整源码包与阶段报告。

### 禁止事项

- 正式业务页面；
- 真实 YUC、AGE 或其他来源接入；
- Redis 强制依赖；
- 用户账户、登录、Session、多设备同步或服务端用户数据 API；
- 远程 GitHub 操作；
- 部署。

### 验收标准

- 工程可在无 Redis、无账户配置下安装、检查、测试和构建；
- 健康页可在本地运行；
- 测试框架和质量脚本可执行；
- 未出现旧项目资产；
- 代码包与基线提交哈希一致可追溯。

### 回滚点

阶段0G文档基线提交。

## 6. 阶段1D——首次GitHub同步与预览部署

### 执行对象

Codex。

### 目标

应用已验收的阶段1代码，完成首次工程提交、推送和健康页预览部署。

### 交付物

- 新旧代码差异报告；
- 安装、类型检查、Lint、测试和构建结果；
- 阶段1提交与推送；
- 健康页预览部署；
- 提交哈希、预览地址和部署报告。

### 禁止事项

- 修改未批准的业务逻辑；
- 添加正式业务页面；
- 引入 Redis、账户系统或真实来源；
- 直接覆盖仓库而不比较差异；
- 跳过检查后提交或部署。

### 验收标准

- 只应用已批准修改；
- 全部阶段1检查通过；
- 预览环境仅展示健康页和必要工程信息；
- GitHub 提交可回滚。

### 回滚点

阶段0G基线提交与上一个可用预览部署。

## 7. 阶段2——全新设计系统、导航和页面骨架

### 执行对象

开发 ChatGPT。

### 目标

使用自建 Mock 数据，从零建立设计系统、响应式导航和首版主要页面骨架。

### 交付物

- 全新设计令牌与基础 UI；
- 桌面端和移动端导航；
- 深色与浅色模式；
- 首页、今日更新、放送表、新番、资料库、详情、收藏页面骨架；
- 北京时间与日本时间切换交互骨架；
- 受控 Mock 数据；
- 组件和页面测试；
- 完整源码包与阶段报告。

### 禁止事项

- 接入真实来源；
- 建立完整数据库模型；
- 用户账户、登录或服务端用户数据 API；
- 使用旧项目组件、页面或设计系统；
- 远程 GitHub操作或部署。

### 验收标准

- 主要页面可使用 Mock 数据完整浏览；
- 响应式、深浅色和基本可访问性可验收；
- 本地收藏仅为前端骨架，不依赖账户；
- 设计不复制旧项目。

### 回滚点

阶段1D已验收提交。

## 8. 阶段2D——设计预览部署

### 执行对象

Codex。

### 目标

将已验收阶段2代码同步到 GitHub 并部署设计预览。

### 交付物

- 差异报告；
- 全量工程检查结果；
- 阶段2提交与推送；
- 设计预览地址和部署报告。

### 禁止事项

- 改变页面业务逻辑或设计决策；
- 接入真实数据；
- 增加账户系统；
- 跳过项目总指挥验收。

### 验收标准

- 预览与已验收代码一致；
- Mock 数据明确标识且不冒充真实资料；
- 可回滚到阶段1D。

### 回滚点

阶段1D提交和部署。

## 9. 阶段3——最小领域模型、PostgreSQL和Mock Repository

### 执行对象

开发 ChatGPT。

### 目标

只实现支撑已验收首版页面所需的最小领域模型、PostgreSQL Schema、Drizzle Repository 和 Mock Repository。

### 交付物

- 最小 Anime、Season、BroadcastInfo、Episode 等必要模型；
- Drizzle Schema 与迁移；
- PostgreSQL 开发与测试配置；
- Mock Repository 和数据库 Repository 统一边界；
- 数据库集成测试；
- 完整源码包与阶段报告。

### 禁止事项

- 一次性实现全部未来模型；
- 创建用户、账户、Favorite、ViewingProgress、RecentView 服务端表；
- 接入真实 YUC 或 AGE；
- 引入 Redis 作为必需依赖；
- 部署或远程 GitHub 操作。

### 验收标准

- 页面可在 Mock Repository 与数据库 Repository 之间受控切换；
- 数据库迁移和回滚可测试；
- 最小模型足够且没有无依据扩张；
- 无账户依赖。

### 回滚点

阶段2D已验收提交和数据库空基线。

## 10. 阶段3D——数据骨架预览部署

### 执行对象

Codex。

### 目标

同步并部署阶段3数据骨架预览。

### 交付物

- 代码与迁移差异报告；
- 安装、类型检查、Lint、测试、构建和迁移验证；
- 阶段3提交；
- 隔离预览数据库；
- 预览部署报告。

### 禁止事项

- 使用生产数据库；
- 接入真实来源；
- 修改已验收 Schema；
- 创建账户数据表。

### 验收标准

- 预览数据库隔离；
- 迁移可重复执行并有回滚点；
- 页面数据骨架可访问；
- 可回滚至阶段2D。

### 回滚点

阶段2D提交、部署和预览数据库快照。

## 11. 阶段4——首页、今日更新、每周放送、季度与未来新番

### 执行对象

开发 ChatGPT。

### 目标

继续使用受控 Fixture 或 Mock 数据，完成首批真实用户流程和时间展示逻辑。

### 交付物

- 首页；
- 今日更新；
- 每周放送表；
- 季度新番、历史季度、未来新番；
- 北京时间默认展示与日本时间切换；
- 跨日、深夜时间、暂定和未知状态处理；
- 测试与完整源码包。

### 禁止事项

- 接入真实 YUC、AGE 或其他来源；
- 使用编造资料冒充真实数据；
- 创建账户系统；
- 部署或远程 GitHub 操作。

### 验收标准

- 核心发现流程完整；
- Mock/Fixture 明确受控；
- 时区换算不覆盖原始值；
- 移动端和桌面端可用。

### 回滚点

阶段3D已验收提交。

## 12. 阶段4D——核心页面预览部署

### 执行对象

Codex。

### 目标

部署阶段4核心发现页面预览。

### 交付物

- 差异和检查报告；
- 阶段4提交与推送；
- 核心页面预览地址；
- 部署报告。

### 禁止事项

- 接入真实来源；
- 修改已验收时间逻辑；
- 将 Mock 数据宣传为真实资料。

### 验收标准

- 预览与阶段4验收成果一致；
- 核心页面可用；
- 可回滚至阶段3D。

### 回滚点

阶段3D提交和部署。

## 13. 阶段5——YUC独立来源评估与接入

### 执行对象

开发 ChatGPT。

### 目标

独立评估并重新实现 YUC 来源接入，不使用任何旧项目代码、规则、Fixture 或 Git 历史。

### 交付物

- YUC 条款、robots、频率、字段和展示边界评估；
- 全新 YUC Adapter；
- SourceRecord、字段映射和 Fixture；
- 限流、失败、冲突和回滚测试；
- 与现有页面的受控数据接入；
- 完整源码包与阶段报告。

### 禁止事项

- 复制或参考旧项目 YUC 实现；
- 绕过访问限制；
- 将 YUC 自动认定为官方；
- 接入 AGE；
- 部署或远程 GitHub 操作。

### 验收标准

- 来源评估获批；
- Adapter 可单独关闭；
- 失败不清空已验证数据；
- 字段来源可追溯；
- 契约测试通过。

### 回滚点

阶段4D提交和禁用 YUC 的配置状态。

## 14. 阶段5D——YUC数据预览部署

### 执行对象

Codex。

### 目标

同步并部署已验收的 YUC 数据预览。

### 交付物

- 来源配置和代码差异报告；
- 全量检查与受控来源验证；
- 阶段5提交；
- YUC 数据预览；
- 部署报告。

### 禁止事项

- 扩大来源字段范围；
- 擅自提高请求频率；
- 启用 AGE；
- 修改业务合并规则。

### 验收标准

- 预览数据可追溯；
- 来源可停用；
- 未突破合规边界；
- 可回滚至阶段4D。

### 回滚点

阶段4D提交、部署和禁用 YUC。

## 15. 阶段6——番剧资料库、搜索和动漫详情

### 执行对象

开发 ChatGPT。

### 目标

完成资料库、PostgreSQL 搜索和动漫详情的正式用户流程。

### 交付物

- 番剧资料库；
- 全局搜索；
- 动漫详情；
- STAFF、CAST、剧集和受控资源展示；
- PostgreSQL Full Text Search 与 `pg_trgm`；
- 搜索解释、筛选、分页和测试；
- 完整源码包与阶段报告。

### 禁止事项

- 引入外部搜索集群；
- 未经批准引入 Redis；
- 接入 AGE；
- 展示未经核验的资源；
- 创建账户系统。

### 验收标准

- 搜索质量和性能达到阶段目标；
- 详情资料来源清晰；
- 外部资源严格按允许类型展示；
- 无账户依赖。

### 回滚点

阶段5D已验收提交。

## 16. 阶段6D——资料与搜索预览部署

### 执行对象

Codex。

### 目标

部署资料库、搜索和详情预览。

### 交付物

- 搜索、Schema 和页面差异报告；
- 全量检查和搜索验证；
- 阶段6提交；
- 预览地址与部署报告。

### 禁止事项

- 擅自调整搜索权重；
- 引入新基础设施；
- 展示未批准 ResourceLink。

### 验收标准

- 预览与已验收搜索行为一致；
- 核心查询稳定；
- 可回滚至阶段5D。

### 回滚点

阶段5D提交和部署。

## 17. 阶段7——AGE独立来源评估、目录与剧集数据接入

### 执行对象

开发 ChatGPT。

### 目标

独立评估 AGE，并只接入获批的目录、分类、年份、更新状态、集数、剧集和其他需核验公开信息。

### 交付物

- AGE 条款、robots、频率、字段和风险评估；
- 全新 AGE Adapter；
- 目录和剧集字段映射；
- 外部页面线索待核验流程；
- 播放与下载资源隔离测试；
- 合并、冲突和回滚测试；
- 完整源码包与阶段报告。

### 禁止事项

- 使用旧项目 AGE 实现；
- 自动导入 AGE 播放线路、下载入口或高风险资源；
- 把 AGE 链接自动发布为 ResourceLink；
- 绕过访问限制；
- 部署或远程 GitHub 操作。

### 验收标准

- AGE 只提供获批候选数据；
- 播放和下载数据无法进入前台；
- 外部线索必须经过独立核验；
- Adapter 可单独关闭；
- 数据来源可追溯。

### 回滚点

阶段6D提交和禁用 AGE 的配置状态。

## 18. 阶段7D——AGE数据预览部署

### 执行对象

Codex。

### 目标

部署已验收的 AGE 目录与剧集数据预览。

### 交付物

- 差异和来源配置报告；
- 全量检查；
- AGE 禁止资源验证；
- 阶段7提交；
- 预览地址和部署报告。

### 禁止事项

- 展示播放线路或下载入口；
- 擅自扩大 AGE 字段范围；
- 改变 ResourceLink 审核规则。

### 验收标准

- 预览只出现获批数据；
- 禁止资源未进入前台；
- 可回滚至阶段6D。

### 回滚点

阶段6D提交、部署和禁用 AGE。

## 19. 阶段8——本地收藏、最近浏览和主动观看进度

### 执行对象

开发 ChatGPT。

### 目标

完成浏览器本地个性化功能，不开发账户系统。

### 交付物

- 本地收藏；
- 本地最近浏览；
- 用户主动设置的观看进度；
- 本地数据版本、校验、迁移、导出和清除；
- 主题和展示时区偏好完善；
- 隐私说明与测试；
- 完整源码包与阶段报告。

### 禁止事项

- 用户账户、登录、Session、多设备同步；
- `user_id`；
- 账户数据库表；
- 服务端用户数据 API；
- 自动推断观看行为；
- 部署或远程 GitHub 操作。

### 验收标准

- 功能在纯本地状态下完整可用；
- 数据损坏可安全回退；
- 用户可清除或导出；
- 不向服务端上传个性化内容。

### 回滚点

阶段7D已验收提交和本地存储版本回退策略。

## 20. 阶段8D——个性化功能预览部署

### 执行对象

Codex。

### 目标

部署本地个性化功能预览。

### 交付物

- 差异和全量检查报告；
- 阶段8提交；
- 个性化预览地址；
- 部署报告。

### 禁止事项

- 增加任何账户或服务端用户数据能力；
- 收集浏览器本地记录；
- 改变用户主动设置原则。

### 验收标准

- 本地功能在预览环境可验证；
- 清除、导出和版本迁移正常；
- 可回滚至阶段7D。

### 回滚点

阶段7D提交和部署。

## 21. 阶段9——全站整合、测试、性能、安全、可访问性与合规验收

### 执行对象

- **开发修复对象**：开发 ChatGPT；
- **部署验证对象**：Codex。

### 目标

完成全站数据整合、缺陷修复、性能、安全、可访问性和合规验收。

### 交付物

开发 ChatGPT：

- 全量缺陷修复代码包；
- 单元、集成、端到端和回归测试；
- 性能和可访问性修复；
- 安全与合规问题修复；
- 数据来源和 ResourceLink 审查结果；
- 阶段报告。

Codex：

- 比较并应用已验收修复；
- 运行全量检查、构建和迁移验证；
- 部署候选版本；
- 输出部署验证报告。

### 禁止事项

- 新增超出首版范围的功能；
- 临时引入账户、Redis、对象存储或外部搜索以掩盖问题；
- 跳过项目总指挥验收；
- 未经批准修改来源和资源边界。

### 验收标准

- 所有阻断缺陷关闭；
- 关键性能、安全、可访问性和合规标准通过；
- 备份、迁移、回滚和来源停用能力验证；
- 候选版本可回滚；
- 项目总指挥明确放行阶段10。

### 回滚点

阶段8D已验收提交和最近稳定预览部署。

## 22. 阶段10——生产准备、正式域名与受控上线

### 执行对象

- **GitHub、部署与上线**：Codex；
- **业务缺陷修复**：开发 ChatGPT，仍须按代码包交接流程验收后由 Codex 应用。

### 目标

完成生产环境、正式域名、受控迁移和正式上线。

### 交付物

- 生产环境配置；
- 正式域名与证书；
- 数据库备份和恢复验证；
- 生产迁移与回滚方案；
- 监控、告警、权利通知和来源停用机制；
- 生产提交、标签和上线报告。

### 禁止事项

- 未获项目总指挥书面或明确指令即上线；
- 擅自改变业务逻辑；
- 把预览密钥或数据用于生产；
- 未审批引入 Redis、对象存储或其他新基础设施；
- 未完成回滚验证即迁移。

### 验收标准

- 阶段9通过；
- 域名、证书、备份、监控、告警和回滚均验证；
- 来源与资源合规清单通过；
- 项目总指挥明确批准上线；
- 上线后检查通过。

### 回滚点

阶段9稳定候选提交、生产数据库备份和上一稳定部署。

## 23. 开发阶段与D部署子阶段关系

适用顺序如下：

```text
Codex 提供当前 GitHub 基线代码包与提交哈希
        ↓
开发 ChatGPT 在代码包上完成当前开发阶段
        ↓
开发 ChatGPT 输出完整源码包、阶段报告和测试结果
        ↓
项目总指挥验收
        ↓ 仅在通过后
Codex 在临时目录比较新旧代码并应用批准差异
        ↓
Codex 运行全部阶段检查
        ↓ 仅在通过后
Codex 提交、推送并执行对应 D 部署子阶段
        ↓
项目总指挥验收部署结果
```

任何一步失败，均不得跳过并进入下一步。
