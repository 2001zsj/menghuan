# 梦幻（Menghuan）技术方案

## 1. 文件性质

本文件记录“梦幻”项目已经确定的首版技术架构与阶段边界，不再以多套候选方案并列表述。

本文件仅修订技术文档，不表示已经创建应用、安装依赖、创建数据库、Git 仓库或部署环境。

## 2. 最终技术决策

“梦幻”正式采用：

- **语言**：TypeScript；
- **Web 框架**：Next.js App Router；
- **运行时**：Node.js；
- **包管理与工作区**：pnpm workspace；
- **数据库**：PostgreSQL；
- **ORM**：Drizzle ORM；
- **运行时 Schema 校验**：Zod；
- **数据同步**：独立 Node.js 同步 Worker；
- **首版搜索**：PostgreSQL Full Text Search 与 `pg_trgm`；
- **单元与集成测试**：Vitest；
- **端到端测试**：Playwright。

以下事项不是首版或阶段1基础依赖：

- Redis；
- 用户账户；
- 登录；
- Session；
- 多设备同步；
- 外部搜索集群；
- 对象存储。

## 3. 总体架构

```text
浏览器
├─ Next.js App Router 页面与服务端查询
├─ 浏览器本地存储
│  ├─ 收藏
│  ├─ 最近浏览
│  ├─ 主动观看进度
│  ├─ 主题偏好
│  └─ 展示时区偏好
└─ 允许名单内的远程图片
        │
        ▼
Next.js Web / BFF
├─ 页面渲染
├─ 公共资料查询
├─ 搜索
├─ 健康检查
└─ 不提供首版用户数据 API
        │
        ▼
PostgreSQL
├─ 业务真相数据
├─ 来源记录与字段追踪
├─ Full Text Search
└─ pg_trgm 模糊匹配
        ▲
        │
独立 Node.js 同步 Worker
├─ Source Adapter
├─ 原始记录与元数据保存
├─ Zod 校验
├─ 规范化、合并和去重
├─ 搜索字段更新
└─ 失败记录与审计

后续按真实性能和并发需求可选引入 Redis
├─ 缓存
├─ 限流
└─ 任务协调
```

架构原则：

- PostgreSQL 是服务端业务真相库；
- 浏览器本地存储是首版个性化数据载体；
- Web 页面不得在用户请求期间临时抓取外部来源；
- Worker 不与页面渲染生命周期绑定；
- 搜索索引必须可从 PostgreSQL 重建；
- Redis 不得成为阶段1或早期页面开发的前置条件。

## 4. 推荐理由

### 4.1 Next.js App Router

动漫资料页、季度页与详情页需要良好的可索引性、首屏速度和分享预览。App Router 支持服务端组件、路由级加载与错误边界、静态生成、动态渲染和按需失效，适合资料型网站。

### 4.2 pnpm workspace

单仓库工作区可以明确隔离 Web、同步 Worker、领域规则、数据库访问、搜索和共享配置，同时避免过早拆成多个独立仓库或微服务。

### 4.3 PostgreSQL、Drizzle 与 Zod

- PostgreSQL 负责关系数据、事务、唯一约束、JSONB、全文搜索和来源追踪；
- Drizzle 提供显式 Schema、类型推导和可审查迁移；
- Zod 负责环境变量、外部数据、接口输入和 Fixture 的运行时校验；
- TypeScript 类型不能代替运行时校验。

### 4.4 独立同步 Worker

外部来源异常、限流、解析失败或结构变化不应拖慢用户页面。独立 Worker 便于单独停止来源、重试、审计和回滚。

### 4.5 PostgreSQL 首版搜索

Full Text Search 与 `pg_trgm` 足以支撑首版作品名、别名、人物和制作信息搜索，减少基础设施数量。只有真实指标证明不足时，才评估外部搜索服务。

## 5. 前端架构

### 5.1 页面范围

Next.js App Router 页面分为：

- 发现：首页、今日更新、每周放送、季度新番、历史季度、未来新番；
- 资料：番剧资料库、搜索、动漫详情、STAFF、CAST、剧集和外部资源；
- 本地个性化：收藏、最近浏览、观看进度；
- 系统：健康页、错误页、来源说明、隐私说明、纠错与权利通知说明。

阶段1只创建工程骨架和健康页，不开发正式业务页面。

### 5.2 渲染策略

- 首页、今日更新：服务端渲染或后续按数据更新策略缓存；
- 季度列表与历史季度：可静态生成并按数据变化失效；
- 动漫详情：可静态生成、按需生成或服务端渲染；
- 搜索：动态服务端查询；
- 收藏、最近浏览、观看进度、主题：客户端读取浏览器本地存储；
- 本地个性化页面不得依赖服务端用户身份。

### 5.3 状态管理

- 服务端公共数据优先通过服务端组件或受控查询层加载；
- 客户端短状态优先使用 React 内建能力；
- 本地个性化数据使用版本化本地存储适配层；
- 只有出现真实复杂度时才引入专用状态库；
- 不把完整来源数据或全量作品数据放入浏览器全局状态。

### 5.4 本地存储边界

首版在浏览器保存：

- 收藏；
- 最近浏览；
- 用户主动观看进度；
- 深浅色主题；
- 北京时间或日本时间偏好。

要求：

- 使用带版本号的存储结构；
- 校验读取数据，损坏时安全回退；
- 支持清除和导出；
- 不创建 `user_id`；
- 不调用服务端同步 API；
- 不在服务端日志记录本地个性化内容。

### 5.5 时间展示

- 默认展示时区：`Asia/Shanghai`；
- 可切换时区：`Asia/Tokyo`；
- 数据层保存来源原始时区、原始日期、原始时间和原始文本；
- 日本“25:00”等深夜表达应保留原文，并另存规范化时间；
- 页面换算结果仅用于显示，不写回覆盖原始数据；
- 时区计算应使用可靠的时区能力并通过单元测试覆盖跨日和夏令时边界。

### 5.6 设计系统与适配

- 所有 UI、组件、样式、图标使用方式和设计令牌必须从零设计；
- 桌面端与移动端同时设计；
- 深色与浅色模式均为首版范围；
- 键盘导航、焦点状态、语义结构、对比度与 `prefers-reduced-motion` 纳入验收。

## 6. 数据获取与同步架构

### 6.1 Source Adapter

每个来源必须拥有独立适配器。推荐的概念接口如下：

```ts
interface SourceAdapter {
  readonly sourceKey: string;
  readonly capabilities: readonly SourceCapability[];
  discover(context: SyncContext): Promise<DiscoveredRecord[]>;
  fetch(record: DiscoveredRecord, context: SyncContext): Promise<RawSourcePayload>;
  normalize(payload: RawSourcePayload, context: SyncContext): Promise<NormalizedSourceRecord>;
  validate(record: NormalizedSourceRecord): ValidationResult;
}
```

YUC、AGE、官方站点和其他来源不得共享旧项目解析器。可以共享的只能是“梦幻”项目内重新开发的 HTTP、重试、限流、Schema 校验、审计和错误分类能力。

### 6.2 同步流程

1. Worker 创建同步批次；
2. 适配器发现待处理记录；
3. 获取来源页面、接口响应或结构化数据；
4. 保存来源元数据、时间、状态、摘要和校验值；
5. 使用 Zod 校验并规范化为 SourceRecord；
6. 执行实体匹配、合并和去重；
7. 写入 PostgreSQL 业务表与字段来源记录；
8. 更新 PostgreSQL 搜索字段；
9. 记录成功、失败、冲突与待处理状态；
10. 后续如存在缓存，再执行精确失效。

### 6.3 阶段性任务协调

在未引入 Redis 时，允许使用：

- PostgreSQL advisory lock；
- PostgreSQL 任务表与唯一约束；
- 单 Worker 串行调度；
- 部署平台提供的受控定时触发。

只有当真实并发、吞吐、限流或任务协调需求证明数据库方案不足时，才由项目总指挥批准引入 Redis。

### 6.4 同步边界

- 页面请求不得临时访问外部来源；
- 来源不可用时仍读取最近一次已验证数据；
- 抓取失败不得把已有字段清空；
- 未通过校验的记录不得进入主业务表；
- 来源适配器必须可单独禁用；
- 不得绕过 robots、登录、验证码、付费墙、地域限制或技术保护措施；
- AGE 播放线路和下载入口不得自动转化为前台资源。

## 7. 缓存策略

### 7.1 阶段1与早期开发

阶段1、阶段2和早期页面开发必须在**没有 Redis**的情况下正常运行。

可使用：

- 浏览器静态资源缓存；
- Next.js 内建数据缓存与路由缓存；
- CDN 或预览平台缓存；
- PostgreSQL 查询优化、索引和必要的预计算；
- 进程内短期缓存，仅限不影响正确性的场景。

### 7.2 Redis 引入条件

Redis 只允许在后续缓存、限流和任务协调阶段，根据真实指标决定是否引入。引入前至少回答：

- 哪个查询或任务存在可测量瓶颈；
- PostgreSQL、Next.js 缓存或部署平台能力为何不足；
- 缓存失效和故障降级如何处理；
- Redis 不可用时系统如何保持核心可用；
- 引入成本、监控和运维责任由谁承担。

### 7.3 缓存原则

- 缓存不是业务真相源；
- 缓存键必须包含数据版本、查询类型、时区、语言和筛选条件；
- 今日更新的缓存周期短于历史季度；
- 来源冲突或资源下线必须支持主动失效；
- 本地收藏、最近浏览和进度不得进入公共缓存。

## 8. 搜索策略

### 8.1 首版实现

正式采用 PostgreSQL Full Text Search 与 `pg_trgm`：

- 对中文名、日文名、英文名、罗马字和别名建立搜索文档；
- 支持 StaffMember、CastMember 和制作信息关联搜索；
- 使用 trigram 支持部分匹配与一定程度的拼写差异；
- 对完全匹配、前缀匹配、别名匹配和模糊匹配设置不同权重；
- 返回命中字段，增强结果可解释性。

### 8.2 规范化

- Unicode 规范化；
- 全角与半角归一；
- 大小写、标点与空格归一；
- 日文假名、罗马字与中文别名独立保存；
- 不用机器翻译自动生成“官方名称”。

### 8.3 升级条件

只有 PostgreSQL 搜索在真实数据和性能测试中无法达到目标，才评估 Meilisearch、Typesense 或 OpenSearch。外部搜索服务即使引入，也只能保存可重建索引，PostgreSQL 仍为真相库。

## 9. 图片策略

### 9.1 首版策略

- 使用允许展示的远程图片；
- 配置明确的图片域名允许名单；
- 对 URL 协议、域名、内容类型、大小和重定向进行限制；
- 无合适图片时使用本项目重新设计的占位图；
- 不批量抓取并永久缓存外部海报；
- 不创建对象存储作为阶段1依赖。

### 9.2 后续审批

后续引入对象存储、长期图片代理缓存、批量海报保存或图片加工前，必须重新审批：

- 图片版权或使用许可；
- 缓存期限；
- 下线与投诉能力；
- 来源署名要求；
- 存储和传输成本；
- 目标域名允许名单与安全策略。

## 10. 部署结构

### 10.1 服务组成

首版受控部署结构：

- Next.js Web；
- 独立 Node.js 同步 Worker；
- PostgreSQL；
- CDN 或托管平台静态资源能力；
- 日志与错误监控。

Redis 和对象存储是后续可选组件，不是首版部署的必备条件。

### 10.2 部署边界

- Web 与 Worker 可独立部署和扩缩容；
- 数据库不得直接暴露公网；
- 预览环境不得使用生产数据库；
- 外部同步在早期预览环境默认关闭；
- 每个开发阶段先由项目总指挥验收，再交给 Codex 执行对应 D 部署子阶段；
- 开发 ChatGPT 不操作远程 GitHub，不部署；
- Codex 不得擅自改变业务逻辑。

## 11. 环境关系

### 11.1 开发环境 Development

用途：开发 ChatGPT 基于 Codex 提供的已验收基线代码包进行开发和测试。

要求：

- 使用 Mock、Fixture 或专用开发数据库；
- 阶段5前不访问真实 YUC；
- 阶段7前不访问真实 AGE；
- 可重置数据；
- 不使用生产密钥；
- 不要求 Redis；
- 不要求账户或认证配置。

### 11.2 预览环境 Preview

用途：Codex 在对应 D 阶段部署已验收成果，供项目总指挥验收部署结果。

要求：

- 使用独立数据库或隔离 Schema；
- 使用受控 Mock、Fixture 或该阶段获准的来源数据；
- 不写入生产数据；
- 不启用未获批准的真实同步；
- 可回滚到上一个已验收部署；
- 不包含账户和登录系统。

### 11.3 生产环境 Production

用途：阶段10受控上线。

要求：

- 独立数据库、密钥和监控；
- 仅由项目总指挥授权 Codex 发布；
- 数据库迁移可回滚或有前向修复方案；
- 来源适配器逐个启用；
- 具备备份、恢复、告警和紧急停用能力；
- Redis 如未被真实需求批准，可不部署。

## 12. 环境变量

### 12.1 阶段1环境变量模板

阶段1只建立必要的 `.env.example`，不提交真实值。建议首批变量：

```text
APP_ENV
APP_URL
PUBLIC_SITE_URL

DATABASE_URL

DEFAULT_DISPLAY_TIMEZONE
ALLOWED_DISPLAY_TIMEZONES

SOURCE_HTTP_USER_AGENT
SOURCE_REQUEST_TIMEOUT_MS
SOURCE_YUC_ENABLED
SOURCE_AGE_ENABLED

SEARCH_INDEX_VERSION

SENTRY_DSN
OTEL_EXPORTER_OTLP_ENDPOINT
LOG_LEVEL
```

其中：

- `DEFAULT_DISPLAY_TIMEZONE` 默认 `Asia/Shanghai`；
- `ALLOWED_DISPLAY_TIMEZONES` 首版仅允许 `Asia/Shanghai,Asia/Tokyo`；
- 阶段1的 `SOURCE_YUC_ENABLED` 与 `SOURCE_AGE_ENABLED` 必须为关闭；
- 所有环境变量在启动时使用 Zod 校验。

### 12.2 首版禁止要求的变量

首版不得要求：

- `SESSION_SECRET`；
- `AUTH_SECRET`；
- 账户服务密钥；
- OAuth 密钥；
- 服务端用户数据同步密钥。

`REDIS_URL`、对象存储密钥和其他可选基础设施变量，只有在对应阶段经批准引入后才可加入，不得预先成为阶段1启动条件。

## 13. 日志、监控与审计

### 13.1 日志字段

服务输出结构化日志，建议包含：

- timestamp；
- environment；
- service；
- requestId / traceId；
- sourceKey；
- syncBatchId；
- entityType / entityId；
- severity；
- eventName；
- errorCode；
- durationMs。

首版没有服务端用户账户，因此不得虚构或要求 `userId`。不得记录密钥、Cookie、令牌、完整外部页面正文或浏览器本地个性化数据。

### 13.2 关键指标

- 页面响应时间和错误率；
- 搜索延迟与零结果率；
- 数据同步成功率；
- 来源响应异常率和解析失败率；
- 冲突记录数；
- 数据库连接数与慢查询；
- 图片加载失败率；
- 外部资源失效率；
- 如后续引入缓存，再增加缓存命中率和缓存错误率。

### 13.3 告警

- 核心页面不可用或错误率突增；
- 数据库不可用；
- 同步连续失败；
- 来源结构大面积变化；
- 搜索数据落后；
- 图片域名或资源链接出现安全异常；
- 权利投诉或紧急下线未及时处理；
- 如后续引入 Redis，再监控 Redis，但不得让 Redis 故障导致核心页面完全不可用。

## 14. 可扩展性

### 14.1 来源扩展

通过独立 Source Adapter 增加来源，不修改核心页面业务语义。每个来源声明可提供的实体和字段。

### 14.2 搜索扩展

搜索包定义稳定查询接口，使 PostgreSQL 搜索未来可替换为外部索引，但不提前引入外部搜索基础设施。

### 14.3 数据扩展

核心实体使用内部稳定主键；外部来源 ID 存入 SourceRecord。新增字段优先使用明确列，不稳定扩展信息可进入受 Zod 约束的 JSONB。

### 14.4 用户功能扩展

首版只有本地个性化功能。未来如提出账户、多设备同步或服务端用户数据，必须重新修改章程、数据模型、安全方案、隐私规则和路线图，不得在现有首版模型中暗中预留账户依赖。

### 14.5 Redis 扩展

Redis 只能作为可选性能与协调组件引入，且系统应具备合理降级路径。Redis 不得取代 PostgreSQL 真相数据。

## 15. 建议项目目录结构

阶段1可按以下全新结构初始化；阶段0R不得创建这些目录：

```text
menghuan/
├─ apps/
│  ├─ web/                    # Next.js App Router Web 与公共 BFF
│  └─ sync-worker/            # 独立 Node.js 同步 Worker 骨架
├─ packages/
│  ├─ domain/                 # 最小领域类型与业务规则
│  ├─ database/               # Drizzle Schema、迁移与 Repository
│  ├─ config/                 # Zod 环境变量和共享配置
│  ├─ search/                 # PostgreSQL FTS 与 pg_trgm 查询边界
│  ├─ ui/                     # 全新设计系统组件
│  ├─ source-core/            # 后续来源适配器公共接口
│  ├─ observability/          # 日志和监控
│  └─ test-utils/             # Fixture 与测试辅助
├─ docs/
│  ├─ architecture/
│  ├─ data-sources/
│  ├─ decisions/
│  ├─ operations/
│  └─ compliance/
├─ fixtures/
├─ tooling/
├─ package.json
├─ pnpm-workspace.yaml
└─ README.md
```

约束：

- `source-yuc` 只能在阶段5创建；
- `source-age` 只能在阶段7创建；
- Redis 专用包或配置只能在获批阶段创建；
- 账户、认证和 Session 包不得在首版创建；
- `.github` 由 Codex 在阶段0G创建和维护，不由开发 ChatGPT操作。

## 16. 测试策略

### 16.1 Vitest 单元测试

覆盖：

- 名称规范化；
- 北京时间与日本时间换算；
- 深夜 24:00 以上时间表达；
- 季度计算；
- 字段优先级；
- 合并与去重；
- 未知值处理；
- ResourceLink 类型与显示资格；
- 本地存储版本迁移和损坏数据回退；
- 搜索权重与查询规范化。

### 16.2 数据库集成测试

使用隔离 PostgreSQL 验证：

- Drizzle Schema 和迁移；
- 唯一约束与外键；
- 事务和失败回滚；
- SourceRecord 与 FieldProvenance；
- Full Text Search 与 `pg_trgm`；
- 并发写入与数据库锁策略。

### 16.3 来源适配器契约测试

每个来源使用固定 Fixture 验证：

- 原始输入可解析；
- 结构变化明确失败；
- 字段来源完整；
- 未知值不被编造；
- 非法数据不污染主表；
- AGE 播放线路和下载入口不会生成前台 ResourceLink。

真实来源验证必须限流并与常规 CI 分离。

### 16.4 Playwright 端到端测试

按阶段逐步覆盖：

1. 健康页可访问；
2. 首页进入今日更新；
3. 放送表在北京时间和日本时间之间切换；
4. 季度筛选进入详情页；
5. 搜索别名找到作品；
6. 本地收藏与取消收藏；
7. 主动设置、修改和清除观看进度；
8. 最近浏览可清除；
9. 外部资源只展示允许类型；
10. 深浅色模式与移动端导航。

### 16.5 非功能测试

- 类型检查、Lint、构建；
- 性能预算与 Lighthouse；
- 基础安全扫描和依赖漏洞检查；
- 可访问性检查；
- 数据库备份恢复演练；
- 来源限流与停止开关演练；
- 远程图片允许名单与异常跳转测试。

## 17. 安全边界

- 所有外部输入、环境变量和本地存储数据使用 Zod 校验；
- 外部 HTML 不直接注入页面；
- 外部 URL 只允许安全协议，并检查域名和重定向；
- 远程图片使用明确域名允许名单；
- 数据库使用最小权限账户；
- 首版不实现登录、Session、账户 Cookie 或服务端用户写接口；
- 不提交真实 `.env`、密钥、Cookie、令牌或数据库文件；
- 不实现绕过来源访问控制的逻辑；
- 不把同步管理能力暴露为公共前台。

## 18. 主要风险

1. 来源条款、robots 与页面结构变化；
2. 多来源名称、集数、时间和状态冲突；
3. 深夜时刻、跨日和时区换算错误；
4. 同名、续作、分割放送和特别篇误合并；
5. 远程图片版权、域名变化和热链限制；
6. AGE 播放与下载资源误进入前台；
7. 多语言搜索召回和排序不足；
8. 本地存储被清理、损坏或版本不兼容；
9. 过早引入 Redis、外部搜索、对象存储或账户系统；
10. 开发代码包与 GitHub 基线不一致；
11. Codex 应用差异时混入未批准变更；
12. 未经验收直接进入部署子阶段。

## 19. 阶段1初始化建议

阶段1由开发 ChatGPT 执行，前提是阶段0G完成且 Codex 提供：

- GitHub 已验收基线的完整代码包；
- 基线提交哈希；
- 当前仓库状态说明。

阶段1只完成：

- Next.js App Router 与 pnpm workspace 骨架；
- `apps/web` 和 `apps/sync-worker` 空骨架；
- TypeScript、Drizzle、Zod、Vitest、Playwright、Lint 和格式化配置；
- `.env.example` 与环境变量校验；
- 健康页和健康检查；
- 基础测试、类型检查和构建脚本；
- 必要的工程文档。

阶段1禁止：

- 正式业务页面；
- 真实 YUC、AGE 或其他来源接入；
- Redis 依赖；
- 用户账户、登录、Session 或服务端用户数据 API；
- 远程 GitHub 操作；
- 预览或生产部署。

阶段1完成后，开发 ChatGPT 输出完整源码包和阶段报告，等待项目总指挥验收；只有验收通过，才可交给 Codex 执行阶段1D。
