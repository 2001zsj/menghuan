# 梦幻（Menghuan）开发规则

## 1. 适用范围

本规则适用于“梦幻”项目的项目总指挥、开发 ChatGPT、Codex、其他参与者、自动化工具、脚本、测试环境、GitHub 仓库和部署环境。

任何人与任何工具均不得以“提高效率”“已有现成实现”“只是参考”“可以先做”为理由绕过本规则。

## 2. 项目独立性

### 2.1 禁止使用旧项目资产

本项目禁止复制、导入、改造、迁移、抽取或参考任何旧项目中的：

- 源代码；
- 组件；
- 页面；
- 数据模型；
- 解析器；
- 同步脚本；
- 样式；
- 设计系统；
- 文档；
- Git 历史；
- 部署配置；
- 测试；
- 项目目录；
- 构建脚本；
- 环境变量命名；
- 数据库迁移；
- CI/CD 工作流；
- 旧项目中的 YUC 或 AGE 实现、Fixture、字段映射和规则。

### 2.2 允许的通用知识

可以使用公开、通用的工程知识，例如 HTTP、TypeScript、Next.js、PostgreSQL、测试和安全原则，但必须在本项目中重新设计具体实现。

不得将旧项目实现细节伪装为通用知识。

### 2.3 新项目身份

- 项目名称：梦幻；
- 英文标识：Menghuan；
- 计划域名：https://www.menghuan.com；
- 所有包名、环境变量、数据库、日志服务名和部署资源使用本项目独立命名；
- 不得保留旧项目品牌、注释、命名空间或远程地址。

## 3. 已确定技术基线

本项目正式采用：

- TypeScript；
- Next.js App Router；
- pnpm workspace；
- PostgreSQL；
- Drizzle ORM；
- Zod；
- 独立 Node.js 同步 Worker；
- PostgreSQL Full Text Search 与 `pg_trgm`；
- Vitest；
- Playwright。

未经项目总指挥批准，不得替换上述核心技术。

## 4. Redis规则

- Redis 不是阶段1强制依赖；
- 工程初始化、设计预览和早期页面开发必须在没有 Redis 的情况下完成；
- 阶段1不得要求 `REDIS_URL`；
- 只有真实缓存、限流或任务协调需求经过测量和评审后，才能由项目总指挥批准引入；
- 引入 Redis 时必须定义故障降级、缓存失效、监控和回滚；
- Redis 不得成为业务真相源，也不得取代 PostgreSQL。

## 5. 首版本地个性化规则

### 5.1 首版禁止账户系统

首版不得开发：

- 用户账户；
- 注册或登录；
- Session；
- OAuth；
- 多设备同步；
- 账户数据库表；
- 服务端用户数据 API。

首版不得要求：

- `user_id`；
- `SESSION_SECRET`；
- `AUTH_SECRET`；
- 账户服务密钥。

### 5.2 浏览器本地存储

以下功能使用浏览器本地存储：

- 收藏；
- 最近浏览；
- 用户主动设置的观看进度；
- 主题偏好；
- 展示时区偏好。

Favorite、ViewingProgress 和 RecentView 可以保留为领域概念，但阶段2和阶段3不得默认实现为需要账户的数据库表。

### 5.3 用户行为真实性

不得创建、伪造或自动推断：

- 收藏；
- 观看进度；
- 已看状态；
- 喜欢或不喜欢；
- 评分；
- 观看平台；
- 观看时间。

点击详情页、剧集资料或外部链接，停留时间和滚动行为均不等于已观看。用户必须能够修改、导出和清除本地数据。

## 6. 时间信息规则

- 默认展示北京时间 `Asia/Shanghai`；
- 允许用户切换日本时间 `Asia/Tokyo`；
- 数据层必须保存来源原始时区、日期、时间和原始文本；
- 页面换算值不得覆盖来源原值；
- 日本深夜 24:00 以上表达必须保留原文并另行规范化；
- 来源时区未知时不得自行猜测；
- 时区逻辑必须有 Vitest 单元测试和 Playwright 用户流程测试。

## 7. 阶段制度

### 7.1 一次只执行一个阶段

任何时候只允许执行项目总指挥明确授权的当前阶段或 D 部署子阶段。

不得因为后续工作“顺手”“简单”或“可以提前准备”而越阶段执行。

### 7.2 未验收不得继续

- 当前阶段未完成验收，不得进入下一阶段；
- 验收有问题时，只允许在当前阶段范围内修正；
- 每个开发阶段完成后，必须先由项目总指挥验收；
- 只有验收通过，才允许项目总指挥向 Codex 下达对应 D 部署指令；
- 不得自行判断“基本完成”后继续。

### 7.3 阶段完成后必须停止

完成当前阶段交付物和汇报后立即停止，不得自动开始下一阶段、安装新依赖、接入来源、操作 GitHub 或部署。

## 8. 三方角色边界

### 8.1 项目总指挥

项目总指挥负责：

- 确定项目范围；
- 下达阶段任务；
- 验收交付；
- 决定返工、放行和回滚；
- 授权 GitHub 和部署操作；
- 批准技术栈重大变更、新来源、新基础设施、账户系统和生产上线。

项目总指挥是阶段流转和远程操作授权的唯一决策角色。

### 8.2 开发 ChatGPT

开发 ChatGPT负责：

- 编写业务代码；
- 编写测试；
- 输出更新后的完整源码包；
- 输出阶段开发报告；
- 标注基线提交哈希；
- 列出修改文件、测试结果和未完成事项；
- 只在当前授权阶段内工作。

开发 ChatGPT不得：

- 操作远程 GitHub；
- 创建、修改或删除远程仓库、分支、标签或保护规则；
- 推送代码；
- 部署预览或生产环境；
- 擅自改变阶段范围；
- 越阶段开发；
- 使用不是 Codex 提供的基线代码包作为开发基础。

### 8.3 Codex

Codex负责：

- 创建和维护 GitHub 仓库；
- 以 GitHub 已验收提交作为唯一代码基线；
- 为每次开发提供当前基线代码包和基线提交哈希；
- 在临时目录比较新旧代码；
- 只应用已验收和已批准范围内的修改；
- 检查差异；
- 运行安装、类型检查、Lint、测试、构建和必要迁移验证；
- 创建提交、分支和标签；
- 推送 GitHub；
- 部署预览和生产环境；
- 输出 GitHub 与部署报告。

Codex不得：

- 擅自改变业务逻辑；
- 擅自修复或扩展未批准代码；
- 在发现代码问题后继续提交或部署；
- 直接覆盖仓库而不比较差异；
- 应用超出项目总指挥批准范围的文件；
- 未经验收直接部署。

Codex发现代码问题、测试失败、基线不一致或范围外变更时，必须停止并报告。

## 9. 代码包交接规则

### 9.1 唯一基线

1. Codex 以 GitHub 中已验收的提交作为唯一代码基线；
2. 每次开发前，Codex 提供当前基线代码包和基线提交哈希；
3. 开发 ChatGPT 只能基于该代码包开发；
4. 聊天中的旧压缩包、旧目录或未验收文件不得替代当前 GitHub 基线。

### 9.2 开发交付

开发完成后，开发 ChatGPT 必须输出：

- 更新后的完整源码包；
- 阶段报告；
- 基线提交哈希；
- 修改文件列表；
- 测试结果；
- 未完成事项。

阶段报告还应说明：

- 本阶段范围；
- 关键实现；
- 已知风险；
- 数据库迁移；
- 环境变量变化；
- 回滚点；
- 是否满足阶段验收。

### 9.3 审查与应用

5. 项目总指挥先审查开发成果；
6. 验收通过后，Codex 在临时目录比较新旧代码，不得直接覆盖仓库；
7. Codex 只应用已批准范围内的修改；
8. Codex 重新运行全部阶段要求的检查；
9. 检查通过后才允许提交、推送和部署；
10. GitHub 仓库是唯一远程真相源，聊天中的压缩包不能替代 Git 历史。

### 9.4 代码包排除项

任何代码包必须排除：

- `.git`；
- `node_modules`；
- 构建产物；
- 缓存；
- 真实 `.env`；
- 密钥；
- 数据库文件；
- 用户数据。

还应排除操作系统临时文件、编辑器缓存、测试报告中的敏感内容和未获批准的大型二进制文件。

### 9.5 基线不一致处置

如开发代码包声明的基线哈希与 GitHub 当前已验收提交不一致：

- Codex 不得尝试自动拼接；
- 不得直接覆盖；
- 必须停止并向项目总指挥报告；
- 由项目总指挥决定重新提供基线、要求返工或授权专门的合并任务。

## 10. 数据真实性规则

### 10.1 不得编造动漫资料

不得编造、猜测或自动补全：

- 动画名称；
- 首播日期和放送时间；
- 集数和剧集标题；
- STAFF、CAST 和制作公司；
- 官方网站；
- 授权播放平台；
- 海报来源和使用许可；
- 资源状态；
- 评分、热度或排名。

未知字段保持未知，并使用明确状态。

### 10.2 不得用推断覆盖来源

- 不根据季度规律推断具体首播日；
- 不根据标题数字推断续作关系；
- 不根据 URL、页面外观或第三方收录推断官方身份；
- 不根据转载内容推断授权关系；
- 不根据总集数自动生成剧集；
- 不用机器翻译结果冒充官方名称；
- 不用页面换算时间覆盖来源原始时间。

### 10.3 来源必须可追溯

同步数据必须尽量保留来源、获取时间、原始标识、解析版本、原始值、采用值和采用理由。人工修正也必须有可核验依据。

## 11. 外部资源规则

### 11.1 首版允许类型

ResourceLink 只允许：

- `official_site`；
- `official_social`；
- `official_video_channel`；
- `broadcaster`；
- `verified_licensed_streaming`；
- `distributor`；
- `encyclopedia`；
- `rating_site`；
- `database`；
- `news`；
- `search`；
- `approved_other`。

`approved_other` 必须有明确审批依据，不得作为无限制兜底。

### 11.2 默认不展示

任何无法验证身份、授权状态或安全性的链接默认不展示。

不得因为页面自称官方、域名相似、搜索排名、第三方收录或使用官方 Logo 就标记为官方。

### 11.3 禁止资源

不得收录或展示：

- 盗版播放地址；
- 网盘链接；
- 磁力链接；
- 种子文件；
- 未经授权的下载地址；
- 规避付费、地域、登录或访问控制的入口；
- 恶意、钓鱼或高风险链接；
- 来源明确禁止聚合的资源。

### 11.4 不托管受版权保护内容

不得托管、转存、批量抓取、永久缓存或分发未经授权的动画视频、漫画、字幕、音频、剧照、海报或下载文件。

## 12. AGE与YUC规则

### 12.1 通用规则

- YUC 和 AGE 仅是未来可能接入的外部公开来源；
- 每个来源必须独立评估条款、robots、数据范围、频率和展示边界；
- 必须重新实现适配器、字段映射、解析器、Fixture 和测试；
- 不得使用旧项目代码、样例、规则或 Git 历史；
- 来源数据不得自动视为官方；
- 来源失败不得清空已验证数据；
- 来源必须可单独禁用和回滚。

### 12.2 AGE候选数据范围

AGE 可以作为以下信息的候选来源：

- 动漫目录；
- 分类；
- 年份；
- 更新状态；
- 集数；
- 剧集信息；
- 外部页面线索；
- 其他需要重新核验的公开信息。

### 12.3 AGE资源禁止自动导入

AGE 页面中的播放线路、下载入口或其他资源不得自动进入“梦幻”前台。

- 播放线路和下载入口不得生成 ResourceLink；
- 外部页面线索先进入待核验状态；
- 只有符合首版允许类型，且身份、授权和安全性均已验证的链接，才可经过批准进入前台；
- AGE Adapter 必须包含禁止资源隔离测试；
- 发现无法可靠隔离时必须停止接入并报告。

## 13. 图片规则

- 首版只使用允许展示的远程图片；
- 必须配置明确的图片域名允许名单；
- 无合适图片时使用本项目重新设计的占位图；
- 不批量抓取并永久缓存外部海报；
- 图片来源和使用状态未知时默认不展示；
- 后续引入对象存储、长期缓存、图片代理保存或批量处理前，必须重新审批版权和缓存策略；
- 图片必须支持下线和投诉处理。

## 14. 代码与架构规则

### 14.1 边界清晰

- Next.js Web 负责页面、公共查询和用户交互；
- 浏览器本地存储负责首版个性化数据；
- 独立 Node.js Worker 负责外部来源获取与同步；
- PostgreSQL 是服务端业务真相库；
- PostgreSQL Full Text Search 与 `pg_trgm` 负责首版搜索；
- Redis 如后续引入，只用于缓存、限流和任务协调；
- 页面请求不得临时抓取外部来源。

### 14.2 不得过早复杂化

未经批准不得：

- 拆分大量微服务；
- 引入多个数据库；
- 引入外部搜索集群；
- 引入复杂消息系统；
- 引入 Redis 作为早期必需依赖；
- 引入账户与认证系统；
- 为未进入路线图的功能预留大量代码。

### 14.3 运行时校验

所有外部数据、环境变量、公开 API 输入和浏览器本地存储数据必须使用 Zod 或同等已批准方式进行运行时校验。

### 14.4 数据迁移

- 迁移进入版本控制；
- 禁止手工修改生产表结构；
- 破坏性迁移必须有回滚或前向修复方案；
- 每阶段形成数据库回滚点；
- 未验证备份不得执行生产迁移。

## 15. 测试规则

- 新业务规则必须有 Vitest 单元测试；
- 数据库约束必须有集成测试；
- 核心用户路径必须有 Playwright 测试；
- 新来源适配器必须有固定 Fixture 契约测试；
- AGE Adapter 必须证明播放和下载资源不会进入前台；
- 修复缺陷时应补回归测试；
- 常规 CI 不依赖不稳定真实外部网站；
- 真实来源验证必须限流并与常规 CI 分离；
- 未通过当前阶段要求的测试不得验收或部署。

## 16. 安全规则

- 不提交密钥、令牌、Cookie、数据库密码、真实 `.env` 或生产配置；
- 不把服务端密钥暴露给浏览器；
- 数据库和内部接口使用最小权限；
- 外部 HTML 不直接注入页面；
- 外部 URL 检查协议、域名和重定向；
- 远程图片使用域名允许名单；
- 日志不得记录敏感凭据或浏览器本地个性化数据；
- 依赖必须经过漏洞检查；
- 发现高危问题必须停止上线；
- 首版不得以安全为名暗中加入账户或 Session 基础设施。

## 17. Git与远程仓库规则

### 17.1 授权边界

只有项目总指挥可以授权：

- 创建 GitHub 仓库；
- 添加或修改远程地址；
- 推送分支；
- 创建 Pull Request；
- 修改仓库可见性和保护规则；
- 删除远程分支或仓库；
- 创建标签和发布。

### 17.2 执行边界

- 阶段0G起，远程 GitHub 操作由 Codex执行；
- 开发 ChatGPT 不执行远程 GitHub 操作；
- Codex只应用已验收成果；
- 不导入旧项目 Git 历史；
- 不改写已共享历史，除非项目总指挥明确授权专门任务；
- 每阶段形成清晰提交和必要标签；
- GitHub 仓库是唯一远程真相源。

## 18. 部署规则

### 18.1 只由Codex执行

预览和生产部署只能由项目总指挥授权 Codex 执行。开发 ChatGPT 不部署。

### 18.2 开发阶段与D阶段

- 每个开发阶段先由开发 ChatGPT交付完整代码包；
- 项目总指挥验收开发成果；
- 验收通过后才向 Codex下达对应 D 部署指令；
- Codex比较差异、运行全量检查；
- 检查通过后才提交、推送和部署；
- 部署结果仍需项目总指挥验收。

### 18.3 禁止擅自部署

未经授权不得：

- 创建预览或生产环境；
- 绑定域名或修改 DNS；
- 创建云数据库、Redis、对象存储或 CDN；
- 创建部署密钥；
- 发布容器；
- 启用定时同步；
- 触发生产迁移。

## 19. 文档与变更控制

- 架构决策使用 ADR 记录；
- 数据来源必须有独立评估文档；
- 字段映射、优先级和冲突规则必须文档化；
- 环境、部署、回滚和事故处理必须文档化；
- 文档与实现不一致时，在当前阶段修正；
- 不得复制旧项目文档。

以下变更必须由项目总指挥决策：

- 新增或删除核心功能；
- 改变非目标范围；
- 更换核心技术栈；
- 新增外部来源；
- 引入 Redis、对象存储或外部搜索；
- 改变图片缓存策略；
- 新增用户账户体系；
- 创建或修改 GitHub 远程仓库；
- 创建预览或生产环境；
- 进入下一阶段；
- 生产上线。

## 20. 异常处置

发现以下情况时必须立即停止并报告：

- 旧项目代码或资产被引入；
- 基线提交哈希不一致；
- 代码包包含排除项；
- Codex比较发现范围外修改；
- 测试、类型检查、Lint 或构建失败；
- 来源条款不明确或禁止当前方式；
- 需要绕过访问控制；
- 数据大量冲突或解析器失效；
- AGE 播放或下载资源进入前台路径；
- 未知资源被标记为官方或授权；
- 用户行为被自动推断；
- 高危安全问题；
- 迁移不可回滚；
- 需要未经授权的 GitHub 或部署操作；
- 当前任务将越过阶段边界。

## 21. 完成与停止规则

每阶段完成后必须：

1. 汇报本阶段完成内容；
2. 列出创建或修改的文件；
3. 列出测试结果；
4. 列出风险与未完成事项；
5. 标注基线提交哈希；
6. 判断是否满足阶段验收；
7. 明确回滚点；
8. 立即停止。

没有项目总指挥新的明确指令，不得继续下一阶段。

## 22. 阶段0R特别规则与停止声明

阶段0R只允许修改：

- `PROJECT_CHARTER.md`；
- `TECHNICAL_PROPOSAL.md`；
- `DATA_MODEL_PROPOSAL.md`；
- `PROJECT_ROADMAP.md`；
- `DEVELOPMENT_RULES.md`。

阶段0R不得：

- 创建第六份文档；
- 创建应用或工程目录；
- 安装依赖；
- 编写业务代码；
- 创建数据库；
- 创建本地或远程 Git 仓库；
- 访问、抓取或接入 YUC、AGE 或其他来源；
- 创建预览或生产部署；
- 开始阶段0G。

完成五份文档修订、检查和汇报后必须立即停止。
